(ns login-demo.login
  #_(:use [purnam.cljs :only [aset-in aget-in]])
  #_(:use-macros
   [purnam.js :only [! def.n obj]]
   [purnam.angular :only [def.module def.config
                           def.controller def.service]]))

#_(def.module loginDemo [])

#_(def.controller loginDemo.MainCtrl [$scope]
  )
